{
  "warnings": [
    "Unknown node types not in schema: 43 × 'method', 11 × 'command', 11 × 'code', 10 × 'component'. These should be mapped to valid schema types (function, file, class, etc.)",
    "42 dangling edges pointing to non-existent nodes - these edges were dropped but indicate incomplete node extraction",
    "Some node IDs still use __nofilepath__ placeholder (e.g., class:__nofilepath__:Settings) but have file_path attributes - IDs should incorporate file paths for uniqueness",
    "Duplicate node type inconsistencies: jarvis.voice.stt.STT transcribe/synthesize appear as both 'method' type and file:-prefixed IDs",
    "Missing cross-file edges: jarvis/main.py imports jarvis/voice/stt.py (from import map) but no edge present for STT class usage",
    "Several test files (test_voxtral_e2e.py, test_voxtral_stt_e2e.py) in root directory not in tests/ package - inconsistent test organization",
    "Configuration nodes use mixed ID schemes: some 'config:filename', others 'file:config_component' - should standardize",
    "Documentation nodes mix file: and module: prefixes inconsistently (e.g., INSTALL_VOXTRAL.md vs VOXTRAL_IMPLEMENTATION.md)",
    "External dependency nodes (voxtral_model, csm_model, kokoro_model) lack version information despite version constraints in pyproject.toml",
    "Some edges have 'type' while others have 'relationship' field - schema inconsistency",
    "web_tools.py missing from node list despite being in import map (jarvis/tools/web_tools.py)",
    "jarvis/voice/stt.py module not represented as a file node, only class/method nodes extracted",
    "Proactive monitors (calendar, email) create graph invocations but edges to run_turn are missing",
    "Settings singleton pattern (get_settings, reload_settings) not clearly represented in graph structure"
  ],
  "suggestions": [
    "Standardize node ID scheme: use consistent format like 'type:filepath:name' for all nodes instead of mixing __nofilepath__ and file: prefixes",
    "Add schema mapping for unknown types: method → function with is_method=true, command → function with is_cli=true, component/code → file with appropriate subtype",
    "Create missing cross-batch edges: main.py → stt.py (STT import/usage), main.py → streaming.py (stream_sentences usage), session.py → tools/web_tools.py (if used)",
    "Add version metadata to external model nodes from pyproject.toml constraints: transformers>=4.48.0, torch>=2.5.0, kokoro-onnx>=0.4.0",
    "Consolidate test file locations: suggest moving test_voxtral*.py to tests/ directory for consistency",
    "Unify edge relationship vocabulary: use either 'type' or 'relationship' consistently, not both",
    "Add import edges from main.py voice loop to STT class and transcribe method (verified in import map but missing from graph)",
    "Create edges from CalendarMonitor.check_once → run_turn and EmailMonitor.check_once → run_turn to show proactive flow",
    "Add web_tools.py file node with exports: web_search, fetch_page (referenced in tool registry but not in graph)",
    "Represent Settings singleton pattern with 'singleton' metadata on get_settings function and 'invalidates_singleton' on reload_settings",
    "Add streaming.py imports to main.py edges (stream_sentences, speak_stream used in _voice_main)",
    "Create hierarchy edges: package __init__.py files should have 'contains' edges to modules in that package",
    "Add tool schema edges: TOOL_SCHEMAS constant should have edges to each tool implementation function it describes",
    "Include environment variable dependencies: nodes like TTS, STT should have edges to their config:env nodes (JARVIS_TTS_ENGINE, JARVIS_STT_LANGUAGE)",
    "Add call stack edges for nested functions in _voice_main: _ask_claude → run_turn, _tts_consumer → TTS.synthesize, etc."
  ],
  "stats": {
    "totalNodes": 224,
    "totalEdges": 213,
    "unknownTypes": 75,
    "danglingEdges": 42,
    "nodeTypeBreakdown": {
      "class": 15,
      "function": 53,
      "method": 43,
      "module": 14,
      "file": 47,
      "constant": 5,
      "config": 11,
      "command": 11,
      "code": 11,
      "component": 10,
      "external": 4
    },
    "edgeTypeBreakdown": {
      "imports": 28,
      "calls": 41,
      "uses": 19,
      "tests": 15,
      "routes_to": 10,
      "contains": 14,
      "depends_on": 9,
      "configures": 11,
      "references": 13,
      "documents": 11,
      "creates": 8,
      "others": 34
    },
    "missingImportEdges": 3,
    "testCoverage": {
      "totalTestModules": 11,
      "coreCoverage": "good",
      "graphCoverage": "good",
      "voiceCoverage": "partial",
      "integrationCoverage": "minimal"
    }
  },
  "criticalIssues": [
    "jarvis/voice/stt.py is in the import map and used by main.py but not represented as a file node in the graph",
    "web_tools.py exists in codebase (from import map context) but completely missing from graph nodes and edges",
    "The main voice loop flow (main.py _voice_main → STT.transcribe → agent.run_turn → TTS.synthesize) has missing edges",
    "Node ID collisions possible: multiple nodes use __nofilepath__ with same class/function names from different files"
  ],
  "positives": [
    "Strong coverage of core architecture: Settings, Toolbox, Agent, Mem0Store all well-represented",
    "Good test node coverage with clear test → implementation edges",
    "Documentation nodes comprehensively mapped including migration guides and specs",
    "Configuration hierarchy well-structured with .env → config components → models",
    "Proactive monitor architecture clearly captured (CalendarMonitor, EmailMonitor, WatchdogMonitor)",
    "Tool dispatch chains properly traced (registry.dispatch → tool implementations)",
    "Google integration flow well-documented (auth → services → calendar/email tools)",
    "Memory system nodes and flow captured (Mem0Store → retrieve/store → graph nodes)",
    "LangGraph state machine nodes properly extracted with think/act/observe/respond cycle",
    "Voice pipeline components present (AudioIO, TTS, STT, WakeWord, streaming utilities)"
  ],
  "recommendations": [
    "HIGH PRIORITY: Add missing STT file node and edges from main.py to resolve critical voice loop gap",
    "HIGH PRIORITY: Standardize node IDs to eliminate __nofilepath__ ambiguity and prevent collisions",
    "MEDIUM PRIORITY: Map unknown node types to valid schema types or extend schema if needed",
    "MEDIUM PRIORITY: Investigate and add missing web_tools.py nodes",
    "LOW PRIORITY: Consolidate edge relationship terminology for consistency",
    "LOW PRIORITY: Add version metadata to external dependencies from pyproject.toml"
  ]
}
